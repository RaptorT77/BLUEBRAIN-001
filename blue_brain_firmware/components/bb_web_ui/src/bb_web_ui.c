#include "esp_log.h"
#include "esp_http_server.h"

static const char *TAG = "BB_WEB_UI";

void bb_web_ui_start(void)
{
    ESP_LOGI(TAG, "Starting Web UI (Captive Portal)");
    // TODO: Init HTTP Server
}
